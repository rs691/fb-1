
// This file is no longer needed and can be removed.
// The real authentication provider is FirebaseClientProvider in `@/firebase/client-provider.tsx`.
// It wraps the application in `src/app/layout.tsx`.
export {};
